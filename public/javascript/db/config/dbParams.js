/**
 * @file db/config/dbParams.js
 * 
 * @overview places the parameters of the API in one place to avoid changing it in every AJAX method
 */
const dbParams = {
    url: "http://localhost:3001",
    route: "API"
}